apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ombi.fullname" . }}-config-pvc
  labels:
  {{- include "ombi.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
  storageClassName: manual