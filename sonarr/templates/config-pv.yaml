apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "sonarr.fullname" . }}-config-pv
  labels:
    pv.beta.kubernetes.io/gid: abc
    type: local
  {{- include "sonarr.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 40Gi
  nfs:
    path: /mnt/config/sonarr
    server: 10.0.1.8
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual